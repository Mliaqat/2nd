    import React from 'react';

    function Uni(props) {

        return(

            <div>

           <h1> My is {props.name} and class is {props.class} in numl</h1>

            </div>

        );
        
    }

    export default Uni;